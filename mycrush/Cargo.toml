[package]
name = "mycrush"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
num = { version = "0.4", features = ["std"] }

[dev-dependencies]
crush-sys = { path = "../crush-sys" }
